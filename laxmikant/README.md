// Clone the repository by the below given command
git clone https://github.com/readychairs/readychairs.git

// Navigate to the folder of the project
cd laxmikant

// Install all the require dependencies by running given command
npm install

// After successfull installation of dependencies run below command to run the project
npm run dev

